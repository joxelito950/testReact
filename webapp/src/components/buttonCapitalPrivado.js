import React, {Component} from 'react';

export default class  extends Component {
    render() {
        return (
            <div id="footer">
              <button type="button" className="btn btn-default" >Nuevo</button>
            </div>
        );
    }
}
